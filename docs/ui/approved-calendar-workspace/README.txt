CAMPAIGN SEAT — APPROVED CALENDAR WORKSPACE

This snapshot protects the approved Calendar interface:

- Day, Week, Month and Agenda
- Event colors and legend
- Search and filters
- Current Eastern Time positioning
- New Event modal
- Event details drawer
- Summary cards
- Mini calendar
- Upcoming events
- Calendar tasks
- Responsive layout

Verify:
  bash "$HOME/Campaign-HQ/scripts/check-approved-calendar-workspace.sh"

Restore:
  bash "$HOME/Campaign-HQ/scripts/restore-approved-calendar-workspace.sh"

Temporarily unlock:
  bash "$HOME/Campaign-HQ/scripts/unlock-approved-calendar-workspace.sh"

Re-lock:
  bash "$HOME/Campaign-HQ/scripts/relock-approved-calendar-workspace.sh"
